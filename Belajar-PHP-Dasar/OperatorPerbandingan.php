<?php

//true jika perbandinganya benar
//false jika perbandinganya salah

// == sama dengan dengan type juggling
// === identik
// != tidak sama dengan
// <> tidak sama dengan + type juggling
// !=== tidak identi// < kurang dari
// <= kurang dari atau sama dengan
// > lebih dar
// >= lebih dari samadengan;

var_dump("10"== 10);
var_dump("10"===10);

var_dump("10" != 10);
var_dump("10" !== 10);
var_dump("10" <> 10);
